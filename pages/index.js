import Layout from "../components/Layout";

// create a home component for the homepage
const Home = () => {
  return (
    <Layout>
      {/* add a static image as per the requirements */}
      <img src="/static/images/logo.png/" alt="portfolio image" />
    </Layout>
  );
};

export default Home;
